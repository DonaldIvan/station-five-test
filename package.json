{
  "name": "station-five-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "install-start-ui": "cd frontend && yarn install && yarn start",
    "ui-start": "cd frontend && yarn start",
    "install-start-api": "cd backend && yarn install && yarn dev",
    "api-start": "cd backend && yarn dev"
  }
}
